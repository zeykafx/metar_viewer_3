import 'package:flutter/material.dart';

class TafPage extends StatelessWidget {
  const TafPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("TAF Page"),
    );
  }
}
