import 'package:flutter/material.dart';

class AirportRunwayInfo extends StatelessWidget {
  const AirportRunwayInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
